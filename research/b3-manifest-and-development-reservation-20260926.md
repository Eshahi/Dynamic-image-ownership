# Actual B3 manifest and development reservation

2026-09-26; issue#10/draftPR#60. Real declared outputs now exist: data/manifest.csv and data/dev-ids.json, not synthetic placeholders. Their engineering acceptance is pending independent exact-artifact review; no scientific or legal acceptance follows automatically.

## Provenance and actual checks

scripts/build_b3_manifest.py consumes exact previously reviewed snapshots: initial5,900-image COCO/DIV2K inventorySHA `bd849697919a1bba5101319ded0401f14be8e8ea150629187fd692389698fe18`, reviewed COCO candidateSHA `37dbd65045497b54a02d14b965824c2155606a0a39d194eced4bc5bdfceff270` and actual-staged DiffusionDB proposalSHA `caa20e7952da822fe1e46b141e59c0cfe03b13d97b79498e909f32cc8315abbd`. Exclusive generated outputs preserve prior artifacts. A failed validator retains its incomplete artifact and must not be counted as success. No earlier output existed at this invocation.

The actual CSV contains6,900 source records:1,000 COCO2017 val,800+100 DIV2K source train/valid,5,000 pre-existing generated DiffusionDB images. Every row names a real relative raw file under the stable data/raw root, source release/split/ID, raw size/SHA/native dimensions, source/terms references and explicit per-item restrictions. Existing native decode evidence applies to byte-identical listed files; the manifest validator freshly re-read every raw size/SHA. Report:6,900 rows/6,900 distinct raw digests, exact domain counts. No cross-source identical bytes among selected rows. This is not canonical/perceptual/user-dependence evidence.

ManifestSHA `5665665b51d5020df00fe1db1dc090a5e8b007c65fda65021c72c6ba709ce71b`,5,756,530 bytes. The provisional group_id is raw- plus that row's raw digest; it certifies only identical-byte grouping. B4 must still merge decoded/near/prompt/user relationships and excluded-image bridges before independent counts or locked splits. The manifest includes public upstream image IDs, not prompt text, source user identifiers or private prompt fingerprints.

All6,900 rows retain pending-image-rights. Local restricted academic-only/no redistribution/public figures/no legal or visual-content certification is explicit. COCO rows record source image license IDs/CC references, not annotation copyright ownership; missing creator/title/current attribution and original archive identity are visible. DIV2K rows retain academic-only/original-owner/source-split caveats. DiffusionDB rows retain curator CC0/terms caveats, strictly-below0.10 image/prompt NSFW metadata screening, no visual review and no raw prompt/user export. Source URLs are provenance references, not a claim that every HTTPS endpoint was freshly fetched. No source-rights verdict or human content check is fabricated. Public figure/publication/final acceptance stays separately gated.

## Development-only reservation

DevSHA `7cc2a4c827fd1c64b2f6d529a3726b843a0c635077982deb87725fbc3ac7f0bc`,14,004 bytes. Reserve32 distinct provisional raw groups:10 COCO,10 DIV2K source-train only and12 DiffusionDB. DIV2K source-validation images are excluded from reservation because A4 plans them for study test. These are not held-out results, sample-size replacement or permission to execute a pilot.

Seed/namespace b3-development-reservation-v1 is fixed before method outcomes. For each domain, sort SHA256(seedUTF8 + NUL + UTF8(domain:release_id:source_split:source_id)), with qualified identity tie-break; choose required counts while skipping already-reserved raw groups. Domain processing is lexical and input permutation-invariant. Every reserved identity/path/hash belongs to the exact manifest bound by itsSHA. B4 must place every later-linked full dependence component in development, even if it contains other source rows; failure to meet fixed study allocation then requires a recorded resolution, not leaking a reservation into validation/test. This artifact does not itself assign remaining study splits or prove independence.

## Tests and known limits

Two focused development tests cover deterministic ordering/input reversal, distinct raw-group selection, source-validation isolation, insufficient capacity and no scientific-approval flags. Literal a/b order was independently checked with .NET SHA256: a=d90d02ca59388b2ea339c608987865ad7c114383fdb72355e5178c7c7ab0d963, b=3d2beda36bd112efd0cba054a6a896cdc14877839e28b06921e06e952d65a813.

Inspection found an existing validator path gap: a directory link within the asset root could pass containment. The validator now checks absolute lexical root/ancestors and every image ancestor for symlinks/junctions before reading. A focused regression is honestly skipped where Windows disallows creating synthetic directory links. Actual files were already independently nonlink-checked; no security policy is changed. Root suite183 tests:172 pass/11 expected dependency/host-link skips; git diff --check passes. No install/model load/dataset download/scientific compute occurred.

Independent deliverable review must distinguish B3's real engineering outputs/local restrictions from any legal/visual/scientific endorsement. It must review missing source archive identity, provisional group IDs and mandatory B4 development isolation rather than treating count checks as science. B5/B4/C2 remain dependency-blocked until actual B3 acceptance; official controller remains paused at plan-acceptance with no verdict.
