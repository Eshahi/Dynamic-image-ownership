# Actual private selected-source staging

2026-09-26; issue#10/draftPR#60. After the actual independent review of exact279ee7b accepted the next local-copy step, root used the reviewed explicit-only staging utility on the exact5,000-member proposal SHA `caa20e7952da822fe1e46b141e59c0cfe03b13d97b79498e909f32cc8315abbd`. No new acquisition, scientific compute or public image/prompt/user export occurred.

Actual stable ignored raw destination: data/raw/diffusiondb-2m/selected-20260926/part-NNNNNN/. It was fresh, absolute and ancestor-link checked before creation. For each approved part, the same verified_archive_snapshot verified original archive/index/inventory identity; stage_selected fully inspected that unchanged in-memory ZIP, matched each explicit name/raw digest and exclusively copied only those unchanged PNG bytes. No ZIP metadata JSON copied and no source transform performed. All14 original ZIPs and unselected alternatives remain intact.

The first invocation failed immediately at module import because the -c invocation lacked scripts/ on sys.path; no directory or image was created. It was repaired by adding the absolute checkout scripts directory to sys.path before imports; this was a command invocation repair, not source/selection/model changes. The successful invocation consumed the bound proposal, grouped selected image_name/raw_sha256 mappings by approved part in fixed order and called stage_selected(...,execute=True). All per-part success receipts and final total are retained in this task's tool output. No failure was silently counted as success.

Final success:5,000 files,3,070,774,949 unchanged raw bytes,14 part directories. Per-part counts in approved order:407,275,359,337,407,407,329,317,388,374,351,392,345,312. The [aggregate receipt](../data/intake/20260926-diffusiondb-selected-staging.json) records storage only. No source freeze/rights/legal clearance/visual review/split/science/B3 acceptance follows. Per-image restrictions in the reviewed proposal still apply; metadata NSFW screening is not visual review.

Post-copy independent actual-file hash/size/membership review is pending. Next coherent B3 work is the actual6,900-row source manifest/provenance/restrictions and32-image development reservation, with independent deliverable review and explicit unresolved limits. Do not re-download, replay completed software/model probes or repeat archive acquisition status. B5 canonical preprocessing and B4 stronger dependence/locked allocation remain separate mandatory tasks; preserve all full source commitments. Scientific compute needs the exact runner approval package, not a storage receipt. Official plan-acceptance remains paused.
