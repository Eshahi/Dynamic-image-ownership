# B1 literature protocol — bounded search

Issue: #8. Request: d916749c-B1-002. Status: prepared for independent completion review; no evidence-gate acceptance.

## Search and selection

Search date/cutoff: 2026-09-23 UTC. No lower publication-year bound and no search-engine recency filter; exclude material first released after the cutoff. Services actually used: public web search, direct arXiv/publisher/author-institution pages, Crossref REST and official GitHub sources. No Scopus/Web of Science subscription database was searched, so no database-completeness claim is made. Existing S01-S36 are a proposal-led search chronology. The time-range disclosure was missing initially and is recorded retrospectively, not represented as a prior preregistration.

Completion-repair query families: semantic/instance content binding; diffusion/latent watermarking; image-domain DCT detection; regeneration. Two broad binding/DCT queries were run before this repair paragraph; their exact strings are retained in S37-S38. For subsequent primary-source selection, include one inspectable image-DCT mechanism and inspect closest nonwithdrawn binding methods discovered. Mark withdrawn work separately rather than use it as validated support. Restrict technical support to primary pages. Record returned relevant leads and inclusion/defer/exclusion reasons; this bounded relevance screen is not an exhaustive database export or systematic review. Unknown fields remain explicitly unknown rather than guessed.

First resolve the six proposal bibliography entries by exact DOI/arXiv identifier against publisher, arXiv, or author-institution pages. Preserve original proposal text; record corrections separately. Inspect methods before assigning comparator roles. Follow primary-source references for latent watermarking, neural extraction, content-dependent keys, regeneration and forgery attacks. Search results are leads, not evidence.

Include methods relevant to the source research questions, and negative evidence about their assumptions. Exclude secondary summaries from technical claim support. Restricted full text remains unavailable unless a lawful public author copy exists. Label metadata, abstract and targeted full-text access separately. Pin revisions, dates and inspected locators. Do not infer novelty from an incomplete search.

For each admitted source produce a strict response card, a local inspection artifact with SHA-256, and typed evidence entries. The hash verifies the local record, not publisher authenticity. Validate and synthesize with the installed literature helper into a fresh directory. Generated metadata/BibTeX still requires human-readable inspection and independent review before technical acceptance.

## Consolidation checkpoint and coverage still required

The root matrix, evidence ledger and working bibliography contain twelve distinct DOI cards and seventeen source-specific entries from B1-002. Eleven inspected sources enter the bibliography; metadata-only Kumari remains visible but is not scientific support. See `literature/final/README.md` for source dispositions and version boundaries. This is a targeted proposal-led search, not an exhaustive systematic review.

- Initial identity/access audit of all six proposal references is recorded in citation-audit.csv; remaining access/version qualifications are explicit.
- Targeted methods and code preflights exist for neural and inversion candidates separately. Final baseline selection and execution readiness remain B2/A5/A6 work, not accomplished by this search.
- Primary regeneration/forgery and qualifying-defense inspections are recorded. claim-evidence-map.csv connects all 42 original IDs to admitted source claims or explicit unmapped status; this does not establish the thesis propositions.
- The chronological query log, six-reference citation audit and root B1 matrix/BibTeX exist. Qualifying lead and narrow later-version screen are now recorded with explicit dispositions; no exhaustive/latest-version result survey claimed.
- Obtain independent evidence review and use the official workflow controller only if the entire evidence gate is satisfied.

## Initial executed inspection

`literature/inspections/seal-v4.md` records a targeted primary-source reading. `literature/seal-seed/` is a deliberately one-paper synthesis. It does not establish performance or settle any IO-01–IO-07 design choice. In particular, a latent-to-image-DCT detection mechanism still needs its own argument and approved feasibility experiment.

## Completion repair and artifact contracts

The root matrix now adds all comparative fields required by guide-plan-38 B1, including separate image_dct/latent_dct/model_free/inversion_free columns. It preserves helper-generated metadata but is no longer byte-identical to the metadata-only generated table. Search rows now expose title/authors/year/doi_or_url/query_id/inclusion_status; citation-audit adds citation_key/verified_fields/source_url/access_depth. Unknown identities for deferred leads are explicit, never bibliography facts. Five thematic queries and their selected/deferred outcomes appear in S37-S51. See literature/final/README.md. The initial independent HOLD audit is retained rather than overwritten.
