# A6 LPIPS candidate software and learned-weight identity receipt

Status 2026-09-24: **offline package import and code/byte provenance passed; no LPIPS metric call or scientific run**. This is a bounded A6 environment transaction under issue #7/draft PR #58, not an accepted quality metric or use-rights determination. It preserves A4's pinned [upstream source commit `082bb24f84c091ea94de2867d34c4544f68e0963`](https://github.com/richzhang/PerceptualSimilarity/tree/082bb24f84c091ea94de2867d34c4544f68e0963) as the comparison target.

The already cached 53,763-byte PyPI `lpips-0.1.4-py3-none-any.whl` was measured again at SHA-256 `fd537af5828b69d2e6ffc0a397bd506dbc28ca183543617690844c08e102ec5e` (the exact [PyPI release's](https://pypi.org/pypi/lpips/0.1.4/json) advertised wheel digest). It was installed **only** into the isolated ignored `.thesis-build/a6-science-venv` with `pip install --no-index --no-deps` from that local file; there was no new package download or dependency replacement. `pip check` returned `No broken requirements found`; import-only `import lpips` and `lpips.LPIPS` lookup passed. The declared reproducible package input is [`requirements-science-lpips-win312-hashes.txt`](../requirements-science-lpips-win312-hashes.txt). `pip freeze` preserves the local wheel URL and its SHA-256. Neither import nor installation constructs a pretrained network.

The installed package's `lpips/pretrained_networks.py` SHA-256 is `6a27f714c51796db466e86bebba6a617c1bfc4d566f3a1756497629c1248686e`, exactly matching the pinned upstream file. Its embedded learned-linear `lpips/weights/v0.1/alex.pth` is **6,009 bytes**, SHA-256 `df73285e35b22355a2df87cdb6b70b343713b667eddbda73e1977e0c860835c0`, Git blob ID `1df9dfe62abb1fc89cc7f82b4e5fe886c979708e`, matching the pinned upstream tree's advertised blob. This learned file is **not** the separate pretrained TorchVision AlexNet trunk already measured in the candidate model-asset receipt.

The installed `lpips/lpips.py` differs at the raw-byte level from the pinned source (wheel SHA-256 `780d09b907cb9b661e0ae28b2d163ddfba92f9e870d7feba34d4790cc6590658`, pinned upstream `3eaeb96c0029b2d849745e6691ccb0d85361bed9cf9953b7ceaed8dc948a6d83`). A read-only comparison fetched that exact upstream source and found its parsed Python AST **equal after removing module/class/function docstrings** from both versions. This supports equal executable structure for that one module; it is not numerical parity, a whole-package equivalence proof or a substitute for checking its imported dependencies. `lpips/__init__.py` remains byte-different, as detailed in [the prior preflight](a6-stage2-metadata-preflight-20260923.md); the installed import path succeeded, but helper behavior outside the LPIPS class is not certified.

The model-free [`scripts/check_a6_lpips_assets.py`](../scripts/check_a6_lpips_assets.py) now fails closed unless the pinned 17-file model-asset lock and all four relevant installed LPIPS package files match exact SHA-256 values, including the learned layer's 6,009-byte length. Its actual offline check returned `status=bytes_only`, `asset_files=17`, and all four declared package hashes; three synthetic verifier tests passed. The complete discovered model-free suite ran 61 tests (59 pass, two expected skips). This is **not** a two-weight loader: it does not instantiate LPIPS or AlexNet, inspect tensor semantics, score an image or test the 63-pixel boundary. Those remain separately authorized future runtime work.

The pinned upstream [LICENSE](https://github.com/richzhang/PerceptualSimilarity/blob/082bb24f84c091ea94de2867d34c4544f68e0963/LICENSE) permits source/binary redistribution subject to notice/disclaimer conditions. The PyPI wheel metadata says `License: UNKNOWN`; this receipt does not infer that every checkpoint or the separate AlexNet trunk carries identical rights. Preserve the upstream attribution/conditions and review weight rights before any redistribution or paper artifact release.

Remaining before scientific LPIPS use: disable/verify implicit pretrained TorchVision downloads, bind the already verified AlexNet trunk and learned layer to a fail-closed loader, test the native 63-pixel minimum and exact image/range normalization, compare a fixed numerical vector across CPU/GPU and chosen source/package route, freeze the image-quality call and failure handling in an exact manifest, and obtain the separate scientific-compute approval. No LPIPS instance, pretrained weight, image metric or diffusion model was loaded for this transaction. A6/#7 stays open.
