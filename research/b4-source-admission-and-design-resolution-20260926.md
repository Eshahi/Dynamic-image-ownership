# B4 prospective source-admission and design resolution

2026-09-26; issue #11 / draft PR #64. This is a pre-outcome data/protocol repair,
not an experiment, reduced source commitment or changed method. Original proposal,
B3 engineering manifest/development reservation, all rejected sources and both
provisional allocation versions remain preserved.

## Authority checked, not inferred from the abbreviated issue title

The immutable `thesis-runs/d916749c/guide-plan-38.json` B4 task includes two
source tasks. `data-2` requires deterministic grouping, unchanged development
reservations, class-specific N reconciliation and explicit shortfalls. `data-3`
requires checking **which claims need cross-model holdout**, and explicitly allows
justified `required=false` where scope does not require it. Its evidence says no
out-of-domain generalization claim without a valid holdout; N/A cannot erase an
approved commitment. The broad GitHub title/objective does not remove this
claim-dependent condition. The source plan itself was not edited.

The actual independent design reviewer `/root/b3_intake_review` inspected
`0247e77ade78ca796002df115cb0c4e6dec0512e` and returned the routes below. This
was advice, not artifact/gate/scientific acceptance. Implementation review remains
required before a final B4 data-plan lock.

## Canonical source admission, preserving the existing ranked population

`research/b3-coco-source-candidates-20260926.md` explicitly permits ordered
reserves with documented pre-freeze exclusions, never selection by method scores.
B3 accepted engineering artifacts, not an irrevocable scientific sample. The
original 1,000-image COCO reference candidate frame includes three native B5
canonical rejects. No image/model/metric outcome has been evaluated. The valid
route is to retain their IDs/reasons and advance the existing fixed reserve order,
not convert grayscale, ignore ICC, rerank the source population or shrink counts.

`scripts/b4_source_admission.py` binds the old manifest/dev hashes, reviewed
candidate frame and exact full canonical inventory/summary. It checks all 1,274
ranked candidate/reserve identities and native dimensions, then admits the first
1,000 canonical-passing records. There are 1,271 canonical-passing records in
this already acquired frame. The three original rejected IDs are 431848, 205289
and 455597; replacement identities are the first three ordered reserves:
233370, 533206 and 397354. All three are singleton components in the previously
screened full graph; no development identity is changed or reranked.

Actual versioned artifacts at `data/b4-admission-20260926/`:

- `source-manifest.csv`: SHA-256
  `40bfeca7c589b8352f93535070bc74fca70c6458216ea5422f0dc173f57110c4`.
- `development-reservation.json`: SHA-256
  `0bfd4178a1929e26d12401000c1eda0c549034c348458844e72f0c5b0a8f79ae`.
- `admission.json`: SHA-256
  `d8240a0ee0b6920727f743b485933a9d919d5ae224309dd9c7c8e334aeab1f6e`.

Fresh local validation rehashed all 6,900 listed files, found 6,900 distinct raw
digests, and preserved counts 1,000 COCO / 900 DIV2K / 5,000 DiffusionDB. Original
32 development source identities, hashes, paths, source labels and raw group IDs
remain byte-for-byte in the reservation's image list; only the new manifest
binding and explicit version provenance are added. Existing old files are not
overwritten. No further acquisition, canonical conversion or model call occurred.
All source rows retain the original pending-image-rights/local-only/publication
restrictions; canonical admission is not content or rights certification.

The three rejects remain in original artifacts and this admission ledger, not
quietly removed from coverage history. Exclusions occur before any scientific
source enrollment, not after unfavorable detector/quality/attack outcomes. The
original engineering frame and its failures remain reportable; no failed
experiment is converted into a success or deleted. Two source-admission tests
check original/reservation preservation, fixed reserves, duplicate/identity and
capacity failures. Full split regeneration/review is still pending.

## Actual N and fixed criteria

A4 `sample-size.md` and `stop-rules.md` already require reporting reduced
independent N, conditional precision and inconclusive findings rather than
fabricating groups, counting seeds as sources, adding unapproved data or relaxing
targets. The 5,000 DiffusionDB image commitment remains unchanged. Its conservative
observed producer/content grouping, not an efficacy outcome, explains the
259/251/757 engineering group allocation. Freeze the prospective representative/
cluster rule and class denominators under independent review before outcomes.
The nominal independent-count shortfalls remain explicit. Validation calibration
uses actual eligible group denominators under the unchanged empirical integer
count rule; selected validation rates are not held-out confidence statements.

The test criterion remains 1% FPR and 80% TPR in every declared domain/negative
stratum, with the existing paired/one-owner/one-seed/failure rules. Neither target
is lowered. At 757 independent eligible test groups, a hypothetical zero-FP 95%
upper bound is about 0.395%; at 251 validation groups it is about 1.186%, but
validation is empirical calibration, not unbiased test inference. These numbers
are conditional design calculations, not measured outcomes or independence
certification. The current uncertainty cannot establish that any method succeeds.

## Cross-model applicability versus ordinary holdout

`research/claims.csv` SCOPE-02 and the exact `scope-matrix.csv` rows require
simultaneous camera-originated/generated source coverage under the same frozen
method, with stratified results. GOAL-01/METHOD-01 require integrated evaluation,
not a claim of transfer to an unseen diffusion checkpoint. The bounded current
profile contains no supported unseen-checkpoint/domain-generalization claim.
An explicitly justified cross-model `required=false` declaration can therefore
satisfy data-3 applicability without deleting an original research question or
relabeling single-model work as cross-model.

The actual 100 DIV2K native source-validation images remain an untouched
source-partition holdout (with full connected components isolated from fitting/
calibration), **not new-domain OOD**. Same-method reporting over all three named
sources remains mandatory. Any future unseen-model/domain claim requires a new
defined excluded fit/calibration domain, trustworthy model-origin identity,
reviewed protocol and applicable resource/scope decision; current DiffusionDB
metadata and the DIV2K partition label cannot supply that evidence.

## Remaining work and failures preserved

The old metadata bridge pins its selected frame to the old manifest, but its
full 14,000 DiffusionDB node snapshot remains unchanged by this COCO-only repair.
The regenerated allocation explicitly authenticates this reuse and binds the admitted
manifest/reservation, not reinterpret old selected-summary counts as new evidence.
Retain all 19,900 observed nodes, including raw-only canonical rejects and excluded
DiffusionDB prompt/producer bridges. The finite-screen limitations and any unresolved
rejected-frame bridge concerns require independent resolution before final lock.
Class-wise source/pair counts, donor/recipient graph policy and conditional-N
precision must be explicit; no future T4 pair protocol is invented here.

The declared `data/splits.csv`, `data/sample-size-check.json`, `data/holdout.json`
and `configs/splits.json` now contain the admitted candidate engineering plan.
CSV SHA-256 `470f803324817706b0b433fd972180acb675436f9cfe01b5bd17252e9364d774`.
All 6,900 selected sources pass unchanged canonical eligibility. The declared
graph still has 7,627 observed/3,167 selected components; the same 259/251/757
DiffusionDB group counts remain, with all nominal image quotas intact. The class
reconciliation contains36 paired source-group cells and12 explicit DiffusionDB
shortfalls, zero observed scientific outcomes, and unchanged FPR/TPR criteria.
Original config bytes are preserved in the provisional artifact directory and
Git history. A duplicate staging allocation is retained in ignored build history.

Design reviewer `/root/b3_intake_review` clarified that rejected observed records
are ineligible provenance nodes, not selected study units with fabricated
fingerprints. The operational graph includes complete near-pair screening over
all19,884 passing nodes, raw links for the16 rejected nodes and allDiffusionDB
metadata/excluded bridges. Unknown relationships remain disclosed; a mathematical
proof of all population independence is not imposed as an invented requirement.
Extra diagnostics are warranted for a concrete unresolved linkage or a stronger
claim, not merely to prove the absence of every possible unknown relationship.
The16 rejection reasons remain inventoried in the grouping companion. This is
design advice; exact regenerated implementation/artifact acceptance is pending.

No final data-plan acceptance is granted by this receipt, issue #11 remains open,
and scientific compute/lifecycle gates remain unchanged. A first read-only reserve
diagnostic assumed key `reserves` instead of actual `ordered_reserves`, and a plan
print failed under Windows cp1252; both were repaired without writing evidence.
These invocation failures are not successful analyses or scientific failures.
