B3 first-batch intake 2026-09-26 (43fd952 / 62d96c1)

The user resumed work and supplied extracted COCO/DIV2K sources plus pinned DiffusionDB metadata personally. All 5,900 local images passed expected filenames, SHA-256/size, native metadata and full decode; COCO dimensions match annotations. Original ZIPs are absent, so their checksums/upstream archive identity are explicitly unavailable. No raw images/prompts were committed or datasets downloaded by the agent.

The exact DiffusionDB metadata size/SHA-256 matched the pinned release; 2M rows and 2,000 x 1,000 part cardinality checked. Only the separately authorized 26.2 MB PyArrow wheel was installed in a metadata-only venv. Prior part-ID/cardinality review blockers were fixed; source-documented NSFW sentinel 2.0 remains excluded and empty text prompts are counted exclusions. Failed intake/metadata attempts are retained locally.

The current eight-part cap correctly blocks with only 3,541 distinct eligible groups. A proposed unchanged-ranking fourteen-part prefix gives 6,219 diagnostic groups; pinned upstream ZIPs total 8,493,745,129 bytes. User resource authorization is pending; no cap change or accepted download list. Independent actor /root/b3_intake_review found no blocker to narrow intake/proposal evidence in exact 43fd952; 62d96c1 addresses stale wording and diagnostic reproducibility warnings, with a narrow re-review pending.

Validation: repository script suite 143 tests (137 passed, six dependency skips); B3 focused science and metadata environments each ran fourteen tests (thirteen passed, one dependency skip). git diff --check passed. No scientific execution or Spec Kit gate transition.

Refs #10; draft PR #60. B3 remains open for DiffusionDB images, final item-level eligibility/rights/content/duplicate controls, manifest and development IDs; dependent B5/B4/C2 are not accepted. Data receipt, replayable capacity diagnostic and review are in research/b3-local-intake-20260926.md, data/intake/ and audits/b3-intake-review-20260926/.
