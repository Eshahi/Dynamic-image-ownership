# A6 full science wheel-candidate inventory

Status 2026-09-25: `requirements-science-candidates-win312.txt` records **42 uniquely identified local candidate wheel SHA-256 values for the 42 installed science-venv distributions**. This is a candidate-byte inventory, **not** a clean scientific environment reconstruction or an accepted lock. Issue #7 / draft PR #58 remains open.

The read-only `scripts/audit_science_wheels.py` now emits the candidate digest(s) for every installed name/version and lists any ambiguous pair rather than silently choosing one. It scanned the existing ignored named-wheel roots, pip HTTP-v2 and built-wheel caches, and the verified interpreter's bundled `pip` wheel. No network, copy, install, model load, GPU operation or study image was used. Against the exact candidate file (SHA-256 `05537bfedd17a093583a4710fad7593ad3770ab14acf52e44b2ccebb10fbd7ef`), the actual report was `cached_wheels_present`: 42 installed, 42 declared candidate hashes, zero missing, zero ambiguous and zero declared hash failures. The 24 pre-existing Stage-2 hashes are an exact subset, asserted by a unit test.

The CLIP candidate is a locally built wheel whose source files and pinned Git-origin metadata were separately checked. The `regex` candidate remains blocked by Windows Code Integrity on this host; presence of its hashed wheel does not make the full CLIP package importable. Several other candidate bytes came from pip cache rather than an independently authenticated package-index receipt. The script does not compare installed payloads for all 42 distributions, resolve platform compatibility on another host, produce a wheelhouse, or prove that an offline clean installation succeeds. The earlier 24-distribution installed-payload audit and CPU workflow clean install remain narrower evidence. Do not use this candidate file as a study compute manifest or close A6 on it.

The repository suite discovered 89 tests: 85 passed and four environment-dependent skips; `git diff --check` passed. One new synthetic test shows multiple candidate hashes are disclosed, and another guards the candidate file's 42 entries and exact Stage-2 subset.
