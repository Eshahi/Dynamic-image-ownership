# First scientific feasibility run: dependency and asset-readiness delta

Status 2026-09-24: **scientific bridge study not executable or authorized**. This is a read-only A6/#7 planning delta, not a new experiment manifest, preregistration, model-acquisition approval, or gate decision. It applies the [research contract](research-contract.md), [scope guard](scope-guard.md), [A5 method](method-spec.md), [A4 acceptance design](acceptance.md), and [approval policy](approval-policy.md). Preserve the hash-bound historical blocked package in `thesis-runs/d916749c/experiments/a5-latent-dct-feasibility/`; do not rewrite it to imply a retrospective executable design. Subsequent to this initial delta, all 17 declared candidate model files passed local byte-identity verification and a separately authorized **no-inference** local component load passed; see the [acquisition](a6-weight-acquisition-receipt-20260924.md) and [component-load](a6-local-model-load-receipt-20260924.md) receipts. Those do not remove the rights, full method/metric, data, resource or scientific-compute approval blockers below.

## Narrow first target

The first scientific target is the **32-source-image development-only latent-to-DCT bridge feasibility check** already allowed by the research contract. It must use existing images, a fixed local image-conditioned diffusion suffix, the two A5 carrier/signature components, matched unmarked reconstruction C0, marked C1, and wrong-owner C2. The decisive observations are on **saved and re-decoded outputs**, using the blind detector's common-q rule and separate C1-vs-C0 and C1-vs-C2 score/failure inventories. Continuous template scores inside the optimizer are diagnostics, not successful detection. Any failed source/seed remains in the denominator. This pilot cannot substitute for the final COCO/DIV2K/DiffusionDB commitments, the 300-per-domain locked-test cells, or a publication claim. The original blocked design's full-study comparator obligations remain later tasks; omitting those comparators from this narrow bridge check does not delete them.

## Changed since the blocked design review

| Dependency | Current evidence | Limit |
| --- | --- | --- |
| A5 algorithm and config boundary | `method-spec.md` and `configs/method.schema.json` now describe the candidate path and mandatory fields. | Algorithm is untested; source/model/numerical parity and full entrypoint are absent. |
| A4 documentary protocol | `acceptance.md`, `sample-size.md`, and `stop-rules.md` now specify the primary and control accounting, a validation-only threshold grid, one-seed schedule, and failure rules. A4/#5 closed as a **DOCUMENT** deliverable after independent review. | No source IDs/splits or completed executable preregistration. |
| Local base software | The isolated Python 3.12 science venv has verified Torch 2.12.1+cu130 and TorchVision 0.27.1+cu130, eight Stage-2 packages, and the exact official OpenAI CLIP source commit. See `a6-stage1-receipt-20260923.md` and `a6-stage2-install-receipt-20260924.md`. | Import checks, synthetic vectors, and a tiny CUDA backward probe are not a model run or full scientific lock. No checkpoint was loaded. |
| Historic design helper | The existing blocked design intentionally uses `draft-blocked-1`, empty datasets and unassigned seeds. | The strict experiment/execution schema still rejects it; an execution manifest must be newly authored and validated after the missing inputs exist. |

## Required before even a local feasibility manifest

1. **Asset custody and rights.** Freeze the exact SD 1.5 model variant and every required component/config/tokenizer/scheduler/safety file, its public source revision, expected size/hash and applicable use terms. Verify downloaded bytes locally through the A6 asset verifier; inspect the unaffiliated mirror's claims and record any unresolved original-release equivalence. Freeze and verify the official CLIP ViT-B/32 checkpoint. For reported LPIPS, the learned LPIPS linear layer and separate pretrained TorchVision AlexNet trunk both need byte/rights receipts; a source-code installation alone is insufficient. Disable implicit downloads in loaders. No model weight was acquired under this delta.
2. **Data identity and leakage control.** B3/B4 must freeze a permissible 32-image *development* source-ID roster (with rights, release/snapshot, raw-byte hashes, content groups, disjoint role/split, native dimensions and failures). Do not pick images from scores or fill exclusions post hoc. Keep the three final-domain commitments untouched. Test images must remain locked; pilot results are exploratory and cannot become confirmatory held-out evidence.
3. **Runnable method and parity.** Implement/review local-only loading, canonical image decode/encode, CLIP preprocessing and feature projection, pHash/DCT, signature/template construction, DDIM image-conditioned gradient path, deterministic base/carriers, output re-decode, and blind common-q detector. Pin exact config and source commit. Test synthetic known answers, a fixed real-path model vector, CPU/GPU numerical drift, safety handling, input minimums, and full gradient continuity. Fail closed on missing/mismatched assets or unsupported images. The A5 32-pixel input minimum does not override the candidate AlexNet/LPIPS 63-pixel minimum.
4. **Resource envelope.** Measure actual peak VRAM/RAM/disk and time on a bounded model-loaded preflight before selecting local versus remote. At 2026-09-24 the host reported 12,227 MiB total / 11,405 MiB free VRAM (driver 610.88) and 32,945,400 KiB total / 16,364,316 KiB free physical RAM; free values are transient, not a workload estimate. The design helper's local 10,240 MiB VRAM cap is a ceiling, not evidence that the method fits. The live RunPod backend remains unsuitable while its enforced termination/deadline behavior is unresolved; no paid budget is approved.
5. **Review and execution authority.** Create a new strict-schema experiment spec and exact execution manifest with data/model/code/config hashes, all seeds, intended output inventory, target host, duration and resource ceilings, estimated cost and failure/stop rules. Obtain independent technical review and the official controller's state-bound plan decision. Then request the user's separate exact-manifest scientific-compute approval through the runner. No blanket approval follows from installing software or acquiring weights.

## Read-only candidate weight inventory, not an asset lock

The pinned unaffiliated SD 1.5 mirror revision `451f4fe16113bff5a5d2269ed5ad43b0592e9a14` advertises four fp16 safetensors LFS objects below. These are **server-advertised claims only**, not local SHA-256 measurements or an adopted variant. The model's small config/tokenizer/scheduler/feature-extractor files require their own complete inventory before acquisition or loading. The mirror [card](https://huggingface.co/stable-diffusion-v1-5/stable-diffusion-v1-5/blob/451f4fe16113bff5a5d2269ed5ad43b0592e9a14/README.md) labels it unaffiliated and names CreativeML OpenRAIL-M restrictions.

| Candidate object | Advertised bytes | Advertised SHA-256 |
| --- | ---: | --- |
| `unet/diffusion_pytorch_model.fp16.safetensors` | 1,719,125,304 | `c83908253f9a64d08c25fc90874c9c8aef9a329ce1ca5fb909d73b0c83d1ea21` |
| `vae/diffusion_pytorch_model.fp16.safetensors` | 167,335,342 | `4fbcf0ebe55a0984f5a5e00d8c4521d52359af7229bb4d81890039d2aa16dd7c` |
| `text_encoder/model.fp16.safetensors` | 246,144,864 | `77795e2023adcf39bc29a884661950380bd093cf0750a966d473d1718dc9ef4e` |
| `safety_checker/model.fp16.safetensors` | 608,018,440 | `08902f19b1cfebd7c989f152fc0507bef6898c706a91d666509383122324b511` |

Those four advertised files total **2,740,623,950 bytes**. The A5-pinned [OpenAI CLIP source](https://github.com/openai/CLIP/blob/d05afc436d78f1c48dc0dbf8e5980a9d471f35f6/clip/clip.py) embeds expected ViT-B/32 checkpoint SHA-256 `40d365715913c9da98579312b702a82c18be219cc2a73407c4526f58eba950af`; read-only HTTP metadata advertised **353,976,522 bytes**. The candidate [TorchVision AlexNet source](https://github.com/pytorch/vision/blob/v0.27.1/torchvision/models/alexnet.py) names `alexnet-owt-7be5be79.pth`; read-only HTTP metadata advertised **244,408,911 bytes**, but upstream's filename supplies only an eight-hex-digit hash prefix, so its *full* local SHA-256 must be measured and independently recorded. These three candidate groups total approximately **3,339,009,383 bytes (3.11 GiB)** before configs, caches, LPIPS's small learned weight and scientific images. On 2026-09-24 the user explicitly authorized this bounded public-weight acquisition and hash verification; that does not adopt the mirror as a proven original release, authorize dataset acquisition, run scientific compute, or buy RunPod time. The 11 small SD component/config/tokenizer/feature-extractor files at the same revision were downloaded and matched to the server-advertised size and Git blob ID. Large-weight transfer is in progress; report completion only after all expected local bytes are hashed.

The first readiness milestone is not a positive result; it is a reproducible, reviewed, locally loadable **negative-or-positive** bridge test package. Until the five prerequisites above are met, report `BLOCKED_EXECUTION`, not an invented scientific outcome or an accepted plan gate.

The explicit [`a6-candidate-model-assets.json`](a6-candidate-model-assets.json) lists the 11 pinned SD small files, four advertised fp16 SD weight objects, official CLIP checkpoint and candidate AlexNet trunk. Its full **listed-files-only** verifier run now passes on all 17 local files, totaling 3,340,603,063 bytes; the lock SHA-256 and limits are in the acquisition receipt. It is neither an independently reviewed completeness/rights decision nor a scientific model lock. AlexNet's measured full local SHA-256 is included, but its official source currently corroborates only the short `7be5be79` prefix.
