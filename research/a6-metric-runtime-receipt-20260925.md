# A6 bounded synthetic metric runtime receipt

Status of the two invocations described below: **LPIPS/AlexNet loaded and produced a fixed synthetic CPU/CUDA scalar; the original CLIP import was blocked before checkpoint loading by Windows Code Integrity. Neither metric passed scientific numerical-parity acceptance.** A later, separately bounded image-only visual-source check succeeded without the unused tokenizer; see [the subsequent receipt](a6-clip-visual-runtime-receipt-20260925.md). The full CLIP package remains blocked. Issue #7 / draft PR #58 remains open.

The user explicitly answered yes on 2026-09-25 to the pending bounded local CLIP and LPIPS/AlexNet model-load question and offered broader access. This receipt applies that answer only to local offline synthetic compatibility checks; it is not an exact-manifest scientific compute authorization, dataset acquisition, paid RunPod budget, security-policy override or approval of a thesis result. Each invocation used an external 1,200-second timeout, the existing isolated science venv, fixed `CUBLAS_WORKSPACE_CONFIG=:4096:8` and `PYTHONHASHSEED=0`, offline Hugging Face settings, the script's socket-connection blocker, pre-load verification of the exact 17-file asset lock (SHA-256 `b8c2595857853ea5b7835bb71f12a34dde07b78623c5e638f692dc7eb220b27e`) and four installed LPIPS files. No study image, diffusion step, dataset, RunPod resource or scientific experiment was used.

## Combined attempt: fail-closed CLIP import

At Git commit `1c6e2685c7484d78e9c2465147c2528023e1c0d6` (script Git blob `62449cc228735485fc9bbc64d3ce2b7f6efe5884`), the command `scripts/check_a6_metric_parity.py --load-metrics` exited **1** before `torch.jit.load`, CLIP preprocessing, CPU/CUDA embedding or LPIPS. Import of the pinned CLIP package pulled `regex`; Windows Code Integrity refused its unsigned native `_regex.cp312-win_amd64.pyd` under an enterprise signing policy. The local CodeIntegrity Operational log recorded IDs **3077** and **3033** for this block. The installed native file's measured SHA-256 was `57ce9fe071fb893a5c79931d23100d4a63f8a92e4f0d567336efc89ee14237b6`. The ignored stdout log was empty (SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`); stderr SHA-256 was `7484511fc471f9d6b1153e411aeb4a3342b491892e86113fffbf2e3d078b3e39`.

This is a **host security-policy boundary**, not a missing COCO image, checkpoint-hash failure or evidence of CLIP numerical failure. We did not disable policy, move/rename the module to evade it, change Python trust settings or substitute unreviewed CLIP source. A sanctioned environment/policy resolution is needed before the intended official CLIP package can be evaluated on this host. Do not claim CLIP load or parity from the already-verified checkpoint bytes.

## Isolated LPIPS/AlexNet attempt

To separate the independent metric branch from CLIP's import failure, `scripts/check_a6_metric_parity.py` gained a fail-closed `--metric {both,clip,lpips}` selection and a model-free test showing `--metric lpips` does not call the CLIP loader. The exact committed execution code was `c2da0fe56be72384b86530313dd50863fa5854f8`, script SHA-256 `58712fcd9d8707a21cdd4f1b89a88a924a2717bd3348a1d71d6e0a71fd1e130f`. The repeated `--load-metrics --metric lpips` process exited **0** under the same external timeout. Its ignored stdout/stderr SHA-256 values were `c493b9cdc11d222890d35380ad6cab0b775796cd1b7a87fbd45ffc993fcb9beb` and `ac7c37d14e14cbc8b727fd274c9883b272b4dcc6b6be622f44c3d3af320ad15b`.

The two separately pinned local AlexNet/LPIPS weight files loaded without an implicit download. For the fixed 64×64 synthetic RGB8 pair, CPU score was `5.447505913025452e-08`, CUDA score `5.9483738823473686e-08`, absolute difference `5.008679693219165e-09`, and peak Torch CUDA allocation `22,909,952` bytes. Internal measured metric time was 1.093 seconds. TorchVision emitted two deprecation warnings about its upstream `pretrained` argument, not a runtime failure. The score is very close to zero; the small absolute difference alone is **not** a robust relative-parity threshold or proof of production LPIPS correctness. The 63-pixel boundary, preprocessing equivalence and nontrivial image-conditioned method path remain untested.

The repository's model-free test suite discovered **82 tests: 80 passed, two expected skips**. No official Spec Kit transition, A6 closure, or scientific result follows from these partial checks. Keep CLIP blocked and LPIPS numerical acceptance open until sanctioned runtime evidence and reviewed criteria exist.
