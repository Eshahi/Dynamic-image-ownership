# Incremental regeneration evidence

Adds one distinct source and three evidence IDs to the seven-card method checkpoint; does not duplicate those cards. Total current corpus: eight distinct DOI records and thirteen evidence entries across `method-checkpoint` and `regeneration`. Earlier `seal-seed` and `proposal-citations` are historical subsets, not additional publications.

Generated from `thesis-runs/d916749c/research/d916749c-B1-002/response-regeneration.json`; helper validation/synthesis pass. Metadata year is first submission, not the inspected revision year. No proof verification, code execution, scientific result or evidence-gate acceptance. Generic BibTeX remains a checkpoint citation.

Relevant proposal IDs: PROB-02, RQ-02, HYP-02 and METRIC-05. Use as threat/design context only. Existing source-to-claim map remains anchored to method-checkpoint until consolidated; these additional relationships are recorded here rather than silently changing its reference namespace. The matched-quality comparison remains untested. Do not equate semantic similarity with satisfying the proposal's pixel-quality targets or assume latent embedding alone evades the theorem.

Theorem 4.3 concerns a false-positive/false-negative tradeoff depending on noise, perturbation and embedding parameters, not deterministic removal for every image. This clarification follows independent review.
