# A6 environment lock and reproducibility boundary

Status 2026-09-24: **clean CPU workflow environment, model-free Stage-1 CUDA wheel probe, bounded deterministic vectors, and Stage-2 software import checks verified; full scientific ML environment blocked**. Issue #7, source task `eng-2`. The [Stage-1 receipt](a6-stage1-receipt-20260923.md) records exact official wheel hashes, the recoverable download failure, the tiny GPU gradient pass and the remaining limits. The [Stage-2 installation receipt](a6-stage2-install-receipt-20260924.md) records exact installed software versions, eight measured direct-wheel digests and offline import checks, but not a complete scientific lock. The [latent-stream vector receipt](a6-deterministic-base-noise-vectors-20260924.md) is a small model-free CPU check, not production numerical parity. The [SD 1.5 custody and loader preflight](a6-sd15-custody-and-loader-decision-20260924.md) identifies a contingent single-file route but does not adopt or validate it. This record applies [research-contract.md](research-contract.md), [scope-guard.md](scope-guard.md), and [method-spec.md](method-spec.md). `requirements.lock` covers existing workflow/contract-validation code only; the Stage-2 pin file is separate. Neither file establishes embedding, CLIP, diffusion, baseline reproduction, full GPU fit, or scientific run readiness. No model checkpoint or dataset was acquired, and no model GPU benchmark or stress test was performed.

## Host observation and isolation

Read-only observation on the user's Windows 11 host: Python 3.12.14; NVIDIA GeForce RTX 5070 Ti Laptop GPU; driver 610.88; `nvidia-smi` reports 12,227 MiB total GPU memory. These values are machine observations, **not** suitability or performance results. The existing `.thesis-build/venv` has `jsonschema==4.26.0` but lacks `torch`, `torchvision`, `diffusers`, `transformers`, Pillow, NumPy and SciPy. It reports installed `thesis-agent-skills==1.0.0` while repository `thesis-agent-skills/pyproject.toml` declares version 1.2.0, so that pre-existing venv is not accepted as a clean project lock verification. Do not install scientific packages into it or change host drivers/security settings.

`scripts/capture_environment.py` provides a read-only, allowlisted JSON capture procedure for both local and future remote hosts. From the checkout being tested, run `& '<verified-python>' scripts/capture_environment.py --project .` and preserve its stdout alongside the experiment manifest. It records UTC time, checkout commit/dirty flag, lock digest, Python/OS/architecture, named package versions (or explicit absence), and `nvidia-smi` GPU identity/driver/memory/compute capability (or an explicit unavailable status). It does **not** enumerate or emit environment variables, host/user paths, model weights or datasets, and does not use network services. Do not treat `git_dirty=false`, a version string, or this inventory as wheel-byte integrity, numerical parity, GPU gradient compatibility, or scientific reproducibility. For this checkpoint the probe reported compute capability 12.0 and 11,501 MiB free of 12,227 MiB at 2026-09-23 13:57 UTC; free memory is transient, and the probe ran in an uncommitted worktree. Four synthetic probe tests passed. Repeat the capture after the code is committed and on each actual execution host.

For the separate science venv, the later [`capture_science_environment.py` receipt](a6-science-environment-capture-20260924.md) records **all** installed distribution versions and six project lock/config digests, rather than the CPU/workflow capture's allowlisted package sample and single lock. It is still a metadata snapshot, not a complete wheel-byte lock or model-method qualification.

The clean CPU environment is `.thesis-build/a6-cpu-venv`, an ignored repository-local venv created from the verified 3.12.14 interpreter. `requirements.lock` pins pip and the exact transitive workflow versions tested by `thesis-agent-skills/requirements-tested.txt`, the official Spec Kit commit in `requirements-spec-kit.txt`, and the local thesis-agent-skills package from this repository. Reproduction must record the checkout commit as well as the lock SHA-256: an editable local package path alone does not identify source bytes. The tested input checkout was `4390b75a3acb7d2525620331a7eef672ee3fd14a`; the verified lock SHA-256 is `c26d467d208f6cfecf6dcfb6ac8a21a18824ff7f7ad56e5bbdfb7adeb6de0aaf`.

From the repository root in PowerShell, without credentials or model downloads:

```powershell
& '.thesis-build/venv/Scripts/python.exe' -m venv '.thesis-build/a6-cpu-venv'
& '.thesis-build/a6-cpu-venv/Scripts/python.exe' -m pip install -r requirements.lock
& '.thesis-build/a6-cpu-venv/Scripts/python.exe' -m pip check
& '.thesis-build/a6-cpu-venv/Scripts/python.exe' -c "import jsonschema, yaml, thesis_agents; print('CPU workflow imports OK')"
& '.thesis-build/a6-cpu-venv/Scripts/python.exe' -m unittest discover -s scripts -p 'test_*.py'
& '.thesis-build/a6-cpu-venv/Scripts/python.exe' -m unittest discover -s thesis-agent-skills/tests -p 'test_*.py'
```

At 2026-09-23 11:06 UTC, the clean install succeeded; the final pip-pinned lock was reinstalled and both test suites rerun successfully. `pip check` reported no broken requirements; imports of `jsonschema`, `yaml` and `thesis_agents` succeeded; installed versions were Python 3.12.14, `pip==25.0.1`, `jsonschema==4.26.0`, `PyYAML==6.0.3`, `thesis-agent-skills==1.2.0` and `specify-cli==1.0.9.dev0`; `specify --version` returned `1.0.9.dev0`. All 18 repository `scripts/test_*.py` tests and all 44 `thesis-agent-skills/tests/test_*.py` tests passed. These are CPU software tests, not model or GPU tests. The install used PyPI and the pinned public GitHub Spec Kit repository; it did not fetch private proposal material. Do not claim offline reproducibility or bit-for-bit wheel identity from version pins alone. A later artifact lock may add package hashes/wheel receipts. If a repeat clean setup fails, record the exact dependency/error and leave A6 open. It is inappropriate to copy an unverified site-packages tree and call it reproducible.

## Scientific runtime not yet locked

The A5 architecture pins official `openai/CLIP` source commit `d05afc436d78f1c48dc0dbf8e5980a9d471f35f6`, with an expected ViT-B/32 checkpoint digest, and Diffusers `v0.35.1` commit `0f252be0ed42006c125ef4429156cb13ae6c1d60`. The official CLIP source and Diffusers distribution are now installed and import without model loading. The [bounded Diffusers source-parity receipt](a6-diffusers-source-parity-20260924.md) shows that the official v0.35.1 annotated tag targets exactly the A5 commit and that five relevant installed modules match its source bytes; this is not whole-wheel or runtime parity. The proposed Stable Diffusion 1.5 model is an unaffiliated mirror revision whose weight identity, rights and equivalence remain unresolved. After the user's bounded acquisition authorization, the CLIP checkpoint's locally measured size and full SHA-256 matched upstream, but it has **not** been loaded or used; see the [asset receipt](a6-weight-acquisition-receipt-20260924.md). Official [CLIP requirements](https://github.com/openai/CLIP/blob/d05afc436d78f1c48dc0dbf8e5980a9d471f35f6/requirements.txt) include PyTorch, TorchVision, ftfy, regex and tqdm. [Diffusers installation guidance](https://huggingface.co/docs/diffusers/v0.35.1/en/installation) recommends isolation. Import success plus the tiny Stage-1 GPU pass does not establish that the proposed model or full method works on this stack.

The A4 quality profile also needs a **two-weight LPIPS runtime**, not just the `lpips` package. Pinned [upstream LPIPS source](https://github.com/richzhang/PerceptualSimilarity/blob/082bb24f84c091ea94de2867d34c4544f68e0963/lpips/lpips.py) loads the `v0.1/alex.pth` learned linear weights and calls the [TorchVision-pretrained AlexNet feature trunk](https://github.com/richzhang/PerceptualSimilarity/blob/082bb24f84c091ea94de2867d34c4544f68e0963/lpips/pretrained_networks.py). GitHub's content API at that LPIPS commit reports `lpips/weights/v0.1/alex.pth` as a 6,009-byte Git blob `1df9dfe62abb1fc89cc7f82b4e5fe886c979708e`; this preflight did **not** locally measure it. Candidate [TorchVision v0.27.1 source](https://github.com/pytorch/vision/blob/v0.27.1/torchvision/models/alexnet.py) names the ImageNet-1K V1 trunk URL `alexnet-owt-7be5be79.pth`; the filename embeds only a hash prefix. Its [documented minimum input size is 63×63](https://docs.pytorch.org/vision/master/models/generated/torchvision.models.alexnet.html), while A5 accepts 32×32 inputs. Before scientific use, pin exact LPIPS/TorchVision package builds, acquire both weights through approved sources, verify full SHA-256 and terms, disable implicit model downloads, and test the native-resolution 63-pixel boundary and numerical parity. Sub-63 source images cannot silently be upscaled for LPIPS. The later [LPIPS software receipt](a6-lpips-software-receipt-20260924.md) records a local learned-weight hash and offline import, while the [candidate asset receipt](a6-weight-acquisition-receipt-20260924.md) records the separate AlexNet trunk; neither is a two-weight numerical metric validation.

Read-only [Hugging Face tree metadata at the pinned mirror revision](https://huggingface.co/api/models/stable-diffusion-v1-5/stable-diffusion-v1-5/tree/451f4fe16113bff5a5d2269ed5ad43b0592e9a14?recursive=true) advertises these LFS SHA-256 object IDs for two **alternative** safetensors variants. These are server claims to verify against downloaded bytes later, not hashes of locally acquired artifacts. The four full-precision files total 5,481,058,520 bytes; the four fp16 files total 2,740,623,950 bytes on disk. Neither figure estimates peak VRAM or backpropagation fit.

| Component | Full-precision safetensors LFS SHA-256 | fp16 safetensors LFS SHA-256 |
| --- | --- | --- |
| UNet | `19da7aaa4b880e59d56843f1fcb4dd9b599c28a1d9d9af7c1143057c8ffae9f1` | `c83908253f9a64d08c25fc90874c9c8aef9a329ce1ca5fb909d73b0c83d1ea21` |
| VAE | `a2b5134f4dbc140d9c11f11cba3233099e00af40f262f136c691fb7d38d2194c` | `4fbcf0ebe55a0984f5a5e00d8c4521d52359af7229bb4d81890039d2aa16dd7c` |
| Text encoder | `d008943c017f0092921106440254dbbe00b6a285f7883ec8ba160c3faad88334` | `77795e2023adcf39bc29a884661950380bd093cf0750a966d473d1718dc9ef4e` |
| Safety checker | `9d6a233ff6fd5ccb9f76fd99618d73369c52dd3d8222376384d0e601911089e8` | `08902f19b1cfebd7c989f152fc0507bef6898c706a91d666509383122324b511` |

The same pinned mirror's single-file `v1-5-pruned-emaonly.safetensors` advertises LFS SHA-256 `6ce0161689b3853acaa03779ec93eafe75a02f4ced659bee03f50797806fa2fa`. The [Comfy-Org archive](https://huggingface.co/Comfy-Org/stable-diffusion-v1-5-archive) claims its corresponding file is hash-identical to the retired RunwayML upload; its [current tree metadata](https://huggingface.co/api/models/Comfy-Org/stable-diffusion-v1-5-archive/tree/9cfd069101959ca3828bf9c04a4419870832b74f?recursive=true) advertises that same object ID. This cross-mirror match corroborates that **single file**, but does not prove equivalence of the Diffusers component files, resolve original-publisher custody, or establish the license of each transformed artifact. The [pinned mirror card](https://huggingface.co/stable-diffusion-v1-5/stable-diffusion-v1-5/blob/451f4fe16113bff5a5d2269ed5ad43b0592e9a14/README.md) labels it unaffiliated and names CreativeML OpenRAIL-M with use restrictions. Exact variant, tokenizer/config hashes, component bytes, terms and safety handling must be frozen before acquisition/execution. No model bytes were downloaded in this preflight.

The Stage-1 PyTorch `2.12.1+cu130` and TorchVision `0.27.1+cu130` pair is now **installed and passed a tiny CUDA gradient check**, but is not a full scientific lock. The official [PyTorch](https://download.pytorch.org/whl/cu130/torch/) and [TorchVision](https://download.pytorch.org/whl/cu130/torchvision/) indexes list Windows CPython 3.12 wheels, and the [TorchVision compatibility table](https://github.com/pytorch/vision#installation) pairs the 2.12/0.27 series. The [PyTorch 2.12 release note](https://pytorch.org/blog/pytorch-2-12-release-blog/) recommends CUDA 13.0+ wheels for Blackwell and gives Windows driver minimum 580.88; observed driver 610.88 exceeds that minimum. The user explicitly authorized this bounded large-wheel installation on 2026-09-23. Neither that authorization nor the observed probe covers full-method gradients, memory footprint, model weights or further large downloads; details and failed-attempt provenance are in the [Stage-1 receipt](a6-stage1-receipt-20260923.md).

The first-stage reproduction commands and model-free CUDA probe are in [scientific-runtime-preflight.md](scientific-runtime-preflight.md). The [receipt](a6-stage1-receipt-20260923.md) records the authorized execution and resolves the older Diffusers installation page's Windows Python-range discrepancy **only for the tested PyTorch/TorchVision imports**, not for Diffusers. The probe is not an experiment and does not validate the full method.

The following remain **unresolved**, despite installed version pins, import passes and the five-module Diffusers byte comparison: full PyTorch/TorchVision/Diffusers/Transformers method compatibility beyond tiny probes; whole-distribution and cross-host parity beyond those five A5-relevant source files; Pillow/LibLCMS image and ICC behavior; NumPy/DCT implementation and numerical parity; complete SD model/tokenizer/scheduler/safety bytes and terms; CLIP feature parity despite its verified checkpoint bytes; LPIPS/SSIM implementation and two-weight checks; a **whole-science-environment** transitive wheel lock beyond the now-hashed 24 Stage-2 transaction wheels; and a remote RunPod image/driver/runtime. A6 must verify actual artifact bytes and method-specific deterministic vectors before scientific manifests. It must separately record model-loading failures, numerical drift and remote differences when a sanctioned environment exists. Driver presence and the tiny gradient pass are not full GPU method qualification. No scientific or paid compute is authorized by this document.

## Offline identity receipt for future approved assets

`scripts/verify_science_assets.py` is a small standard-library, **model-free** verifier for files acquired later under a separate decision. Its lock format is `{"schema_version":"a6-asset-lock-v1","files":[{"path":"model/weights.safetensors","size_bytes":9,"sha256":"<64 lowercase hex characters>"}]}`; the 9-byte entry is schematic, **not** a real checkpoint lock. Paths are relative to an explicit asset root. The verifier rejects absent, size-mismatched, hash-mismatched, linked/junction and traversal paths; it does not enumerate a directory or access the network. For a future reviewed lock, invoke `& '<verified-python>' scripts/verify_science_assets.py --asset-root '<approved-local-assets>' --lock '<reviewed-lock.json>'` and retain the JSON receipt and lock SHA-256 with the exact run manifest. Synthetic tests do not use model weights.

The output explicitly says `listed-files-only`: a pass does **not** prove that every required model/config/tokenizer/safety file was listed, that advertised upstream hashes are authentic, that rights are cleared, or that weights can load and produce correct gradients. The lock must be independently reviewed for completeness and source/rights before it can support A6 acceptance. It does not replace compute approval.

## Determinism-setting preflight

The [model-free PyTorch 2.12 setting receipt](a6-torch-determinism-preflight-20260924.md) records a fresh local process with fixed launcher variables, strict deterministic-algorithm mode, cuDNN benchmarking disabled and IEEE float32 backend settings. It explicitly leaves NumPy/other generators, actual model operations, cross-device parity and performance unresolved. Do not use its `settings_applied` result as evidence that the scientific method is deterministic. The future runner must apply and capture the reviewed profile in the exact execution process, then test the real path rather than inheriting a prior preflight result.

The [fixed model-free CUDA DCT/autograd receipt](a6-dct-cuda-probe-20260924.md) adds a same-host two-process fingerprint check and a scalar CPU comparison for one tiny A5-frequency vector under that profile. It is bounded Stage-1 wheel compatibility evidence, not the full DCT detector, diffusion gradient, image experiment, VRAM fit or cross-host reproducibility.

The Stage-2 science venv also has the exact official OpenAI CLIP **source package** pinned in [`requirements-science-clip.txt`](../requirements-science-clip.txt); see the [software-install receipt](a6-stage2-install-receipt-20260924.md). Its import works without a checkpoint. This is not the ViT-B/32 weight receipt or a feature-extraction parity test. Calling upstream `clip.load` with a model name can download a checkpoint implicitly; scientific code must use an independently verified local file and reviewed loading path.

## Current acceptance limit

A6/issue #7 may only close when the clean setup and CPU import/unit-test evidence exists **and** the declared scientific environment is resolved or its explicit blocked scope is accepted under the issue's completion criteria. The CPU workflow and Stage-1 CUDA compatibility checks are genuine bounded progress, not task completion. The official Spec Kit `plan-acceptance` gate remains pending independent evidence review; this record alone cannot advance it.
