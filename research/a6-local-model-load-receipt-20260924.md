# A6 bounded local SD component-load receipt

Status 2026-09-24: **offline component load passed; no image inference or scientific experiment**. The user explicitly approved the separately requested local compatibility preflight in the authenticated Codex task on 2026-09-24. The approved boundary was: a synthetic/no-dataset local load and RTX 5070 Ti memory check, at most 15 minutes, without RunPod cost, new packages or new weight downloads. The implementation was narrower: it performed no synthetic-image call at all. This approval is not an exact-manifest scientific-compute approval, model/rights acceptance, or a Spec Kit gate verdict.

## Exact local check

- Worktree branch: `codex/7-environment-lock`; issue #7, draft PR #58. Python: ignored isolated `.thesis-build/a6-science-venv/Scripts/python.exe` on the project drive, using installed Torch `2.12.1+cu130`, TorchVision `0.27.1+cu130`, Diffusers `0.35.1` and its pinned Stage-2 dependencies.
- Script: [`scripts/check_a6_local_model_load.py`](../scripts/check_a6_local_model_load.py) with `--asset-root W:/Prrojects/image ownership/THESIS_GUIDE_OFFLINE_v5/.thesis-build/assets/a6`, `--lock research/a6-candidate-model-assets.json` and explicit `--load-model`. A parent PowerShell process applied a 900-second timeout to the child and redirected its output to ignored `.thesis-build/logs/a6-model-load-20260924.{out,err}.log`. The child exited **0** in about 15 seconds of outer wall time, well before the ceiling.
- Before model import, the helper reverified all **17** declared files by exact size/SHA-256, rejected missing/unlisted files in the exact 15-file SD component directory, set Hugging Face/Transformers/Diffusers offline environment variables, and blocked in-process socket connections. It called `StableDiffusionImg2ImgPipeline.from_pretrained` on the local directory with `variant="fp16"`, `use_safetensors=True` and `local_files_only=True`, checked non-null safety checker and feature extractor, then moved the pipeline to CUDA. It did **not** call the pipeline for image inference, save an output, retrieve study data, run a gradient or download anything.
- Recorded JSON: `status=model_loaded_no_inference`, `verified_files=17`, `pipeline_class=StableDiffusionImg2ImgPipeline`, `scheduler_class=PNDMScheduler`, `safety_checker_loaded=true`, `feature_extractor_loaded=true`, CUDA device `NVIDIA GeForce RTX 5070 Ti Laptop GPU`, total VRAM **12,820,480,000 bytes**, free immediately before component load **11,578,376,192 bytes**, free after transfer **8,757,706,752 bytes**, peak Torch allocation **2,765,889,536 bytes**, model-load timer **2.734 seconds**. These are one transient host observation, not a guaranteed capacity envelope. The process exited; subsequent `nvidia-smi` returned to 307 MiB used / 11,638 MiB free.
- Stderr showed only the seven-component progress indicator and Diffusers' `torch_dtype` deprecation warning; there was no observed missing-file, network or load error. The warning is not a failure but should be removed after confirming the supported `dtype` API, without altering this receipt.

## What this does not establish

The pipeline object can load from the locally verified **candidate** mirror components with safety pieces present, but no input passed through it. This does not prove that the mirror equals the retired publisher release, that the model's use rights are cleared, that safety classification behaves correctly, that CLIP or LPIPS numerics agree with A5/A4, or that the A5 image-conditioned DDIM reverse suffix is implemented. A 2.77 GB peak *weight-load allocation* does not bound gradient activations, optimizer state, full method VRAM, native image resolution, runtime, determinism or a remote RunPod host. No dataset ID/split, scientific outcome, acceptance gate, paid budget or exact experiment manifest was used. A6/#7 remains **open**; B3/#10 and the first 32-image development-only bridge study are not yet unlocked.
