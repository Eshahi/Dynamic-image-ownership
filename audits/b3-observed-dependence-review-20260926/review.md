# Independent observed dependence review

Date: 2026-09-26. Author `/root`; actual independent read-focused actor `/root/b3_intake_review`, under `AGENTS.md` and `research/approval-policy.md`. Exact reviewed commit `eb187ed2d96c3cb2e456351ae660da230f986eb0`. This records the returned report, not a human or scientific verdict.

No blocking finding for narrow partial raw-byte/exact-prompt connectivity and completed archive integrity. Four focused tests passed. The reviewer independently matched the private eligibility ledger and six CSV digests, replayed the helper in memory and reproduced output SHA-256 `1b60be9d40172262c3584db9e9b2e23cb7a824f908c3fc33f9cf11b9d3eab579`.

The reviewer separately constructed graph adjacency and breadth-first connectivity, not the helper's union-find, matching every component's members and independently checking IDs. Reproduced 6,000 records, 2,962 eligible, 3,035 score/size exclusions, three empty prompts, 2,747 components, 138 multi-image components and 12 all-observed raw duplicate groups. Complete fourteen-part coverage is correctly false. The implementation transitively combines both edge types, separates their hash namespaces, binds snapshots, reuses complete-part/identity checks and preserves excluded records without deletion.

The reviewer also independently replayed completed ZIPs1929/1168: exact archive sizes/hashes, all member CRCs, all 1,000 native PNG verification/full decodes/dimensions, aggregate fields and per-image CSV digests matched. Root full workflow suite passed 172 tests (164 pass/eight expected dependency skips); `git diff --check` passed.

Non-blocking but mandatory later boundary: components cover the **eligible observed-node subgraph**. Excluded nodes are not retained as bridging vertices. They must not be reused as final B4 independence/split groups without a separately reviewed policy handling excluded bridges and other dependence sources. Membership-bound IDs can change as later observed nodes merge components; no final stable IDs or independence claim is accepted.

Accepted scope is this bound partial metadata/raw-byte connectivity and the two new completed archive receipts only. Content/rights, canonical/near/user dependence, final source IDs/manifest/splits, B3 closure, official gate transition and scientific compute remain unaccepted. The reviewer wrote no tracked files, inspected no partials and changed no acquisition process.
