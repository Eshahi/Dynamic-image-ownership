# Experiment-design prerequisites

2026-09-23. This is a dependency/readiness note, not a preregistration or execution authorization.

Current real evidence response: research/d916749c-B1-002/response-final.json (relative to this run directory). Root literature outputs and independent audits are the research evidence; earlier BLOCKED_INPUT prompt handoffs are historical. Review authority is research/approval-policy.md.

The 38-task graph places A5/issue #6 before A4/issue #5. A5's method-spec.md and configs/method.schema.json are not yet produced. Required design obligations include an existing-image route, stable key recomputation and an explicit latent-to-image-DCT mechanism. A3 marks IO-01 through IO-07; the method must apply research/scope-guard.md. Do not substitute inversion, a neural detector or image-space embedding for the proposed path.

If the lifecycle's experiment-design prompt runs before these prerequisites exist, record the exact dependency and a draft/handoff only. Do not manufacture a runnable manifest, dataset release, threshold, resource estimate or positive feasibility result merely to make the prompt complete. Root work can continue A5 independently; later actual preregistration/compute package must reference reviewed implementation and exact artifacts.

No scientific runs are approved or expected yet. Ordinary static/unit/mock tests remain authorized. A completed prompt is not A4/A5 acceptance, and the plan-acceptance gate must remain pending until its real required outputs pass independent review.
